# frozen_string_literal: true

source 'https://rubygems.org'

ruby '~> 2.6.0'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}.git" }

gem 'every-politician-scraper', '~> 0.0.9', github: 'tmtmtmtm/every-politician-scraper'
gem 'open-uri-cached'
gem 'pry'
gem 'rake'

group :test do
  gem 'reek', '~> 6.0'
  gem 'rubocop', '~> 0.89'
end
